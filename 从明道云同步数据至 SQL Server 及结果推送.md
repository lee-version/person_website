# 从明道云同步数据至 SQL Server 及结果推送

**【简历短模块·项目介绍】**

搭建**明道云低代码平台 \+ SQL Server**食堂异物率自动化分析体系，基于Python开发全流程数据自动化脚本，打通「低代码API数据拉取、SQL Server数据清洗入库、异物率指标聚合计算、数据回传可视化」全自动化闭环。自主封装通用数据库、明道云API交互工具类，通过批量分批插入、事务回滚、多格式兼容校验优化程序稳定性，可稳定批量处理全量食堂运营数据。彻底替代人工导出、核对、统计、上报的繁琐流程，**省去每日人工统计耗时2小时\+，报表产出****和可视化****效率提升90%以上，杜绝人工核算误差，数据统计准确率提升至100%**，实现多站点食堂异物投诉率、就餐人次数据的自动化分析与实时可视化看板展示，为食堂运营整改、品质优化提供精准数据支撑。
核心技术：Python、Pandas、pymssql、Requests、SQL Server、明道云API、数据清洗与聚合。

这些代码整体实现了**从明道云低代码平台同步异物投诉、就餐人次数据到 SQL Server 数据库，统计异物投诉率，并将统计结果推送回低代码平台** 的完整流程，涉及数据库操作、数据清洗统计、API 交互等核心功能。以下分文件详细解析：

### 一、整体流程概览

1. **数据拉取**：从明道云平台获取「异物投诉」「就餐人次」原始数据，写入 SQL Server 数据库；

2. **数据统计**：从数据库读取原始数据，按「站点 \(Site\)\+ 年 \+ 月」聚合，统计异物投诉次数、就餐总人次；

3. **数据推送**：将统计结果写入数据库专用表，并推送回明道云低代码平台；

4. **辅助能力**：包含数据库连接、邮件发送、日期处理、批量操作等工具函数。

### 二、各文件功能解析

#### 1\. `test\_insert\_sql\.py` \- 核心：从明道云同步原始数据到 SQL Server

**功能**：将明道云平台的「异物投诉」「就餐人次」原始数据拉取并写入 SQL Server 的`ods\_complaints`（异物投诉）、`ods\_diningperson`（就餐人次）表。

- **关键配置**：

    - 明道云鉴权（APP\_KEY/SIGN）、数据表 ID（WORK\_ID）、字段映射（columns\_id/columns\_pd）；

    - 数据库连接信息（从`password/data\.json`读取）。

- **核心逻辑**：

    1. 初始化`MDY`类（明道云交互类），调用`get\_all\_lines\_byIndex`分页拉取明道云数据；

    2. 数据清洗（日期格式转换、数值类型修正）；

    3. 自动建表 \+ 清空表（避免重复数据）；

    4. 批量插入数据到 SQL Server（`batch\_execute`分批处理，默认每批 1000 条）。

#### 2\. `test\_return\_plantform\.py` \- 核心：统计数据并推送回明道云

**功能**：读取 SQL Server 中的原始数据，按维度统计后写入专用表`ods\_complaints\_rate`，并将上月统计结果推送回明道云。

- **关键模块**：

    - **工具函数**：

        - `get\_year\_month`：从日期提取年 / 月（兼容字符串 / 日期类型）；

        - `read\_sql\_to\_df`：封装 SQL 查询结果为 DataFrame（适配 MSSQLOBJ 返回格式）；

    - **主逻辑**：

        1. 连接 SQL Server，读取`ods\_complaints`/`ods\_diningperson`表数据；

        2. 按「Site \+ 年 \+ 月」聚合：统计异物投诉次数、就餐总人次；

        3. 合并两张表数据，填充空值、格式化日期列；

        4. 写入`ods\_complaints\_rate`表（自动建表 \+ 清空 \+ 批量插入）；

        5. 筛选上月数据，调用`push\_to\_lowcode\_platform`推送回明道云；

    - **推送逻辑**：初始化`MDY`类，调用`post\_all\_intern\_batch`分批（每批 50 条）推送数据。

#### 3\. `tools\_api\.py` \- 基础工具库：数据库 / 邮件 / 日期等通用能力

**核心类 / 函数**：

- **`MSSQLOBJ`**：SQL Server 数据库操作类，封装：

    - `execute\_query`：执行查询并返回结果；

    - `execute\_non\_query`：执行增删改（无返回值）；

    - `executemany\_non\_query`：批量执行增删改；

    - `batch\_execute`：按批次处理数据（避免单次操作数据量过大）；

- **`DatabaseOBJ`**：通用数据库连接类（适配 PostgreSQL，未在主流程使用）；

- **`csv2email`****/****`csv2email\_multiAttachment`**：邮件发送类（支持单 / 多附件、空数据提示）；

- **`get\_time`**：获取当前 / 上月 / 下月的年 / 月（核心用于筛选上月统计数据）；

- **`get\_db\_password`**：从`password/data\.json`读取数据库连接配置。

#### 4\. `excute\_mdy\.py` \- 明道云交互核心类：MDY

**功能**：封装与明道云低代码平台的 API 交互，实现数据拉取、批量推送。

- **核心方法**：

    - `get\_all\_lines\_byIndex`：分页拉取明道云数据表数据（循环请求直到无数据）；

    - `post\_all\_intern\_batch`：按批次拆分 DataFrame，调用`post\_all\_intern`推送；

    - `post\_all\_intern`：构造明道云批量新增 API 的请求参数，发送 POST 请求；

- **关键配置**：明道云 API 地址、请求头、字段映射（columns\_id/columns\_pd）。

#### 5\. `webhook\.py` \- 辅助：Webhook 推送（未在主流程使用）

- 定义`webhook`类，`after\_Push`方法可向指定 URL 发送 POST 请求（携带 site/month/msg），用于数据推送后的回调通知（当前代码未调用）。

### 三、核心依赖与技术栈

1. **编程语言**：Python 3\.x；

2. **核心库**：

    - `pandas`：数据清洗、聚合、格式转换；

    - `pymssql`：SQL Server 数据库连接；

    - `requests`：HTTP 请求（调用明道云 API、Webhook）；

    - `sqlalchemy`：通用数据库连接（备用）；

    - `smtplib`：邮件发送；

3. **交互平台**：明道云低代码平台（API 交互）；

4. **数据库**：SQL Server（行政库`xingzheng`）。

### 四、关键设计亮点

1. **批量处理**：所有数据插入 / 推送均按批次处理（避免单次操作超时 / 数据量过大）；

2. **容错性**：

    - 数据库操作带事务回滚（出错时`rollback`）；

    - 日期格式兼容（字符串 / 日期类型自动转换）；

    - 空值填充、数据类型强制转换；

3. **可扩展性**：

    - 字段映射通过配置（columns\_id/columns\_pd）实现，新增字段只需修改配置；

    - 数据库连接配置解耦（从 json 读取，不硬编码）；

4. **自动化**：自动建表、清空历史数据，无需手动维护表结构。

### 五、典型使用场景

- 定时执行`test\_insert\_sql\.py`：同步明道云原始数据到数据库；

- 定时执行`test\_return\_plantform\.py`：统计数据并推送回明道云；

- 扩展：结合`csv2email`可将统计结果以邮件形式发送。

> （注：文档部分内容可能由 AI 生成）
