# 滴滴用车费用对账单自动化生成与邮件发送工具

**【简历短模块·项目介绍】**

运用Python、Customtkinter、Pandas、SMTP协议开发企业滴滴费用对账可视化自动化工具，搭建GUI可视化操作界面，实现订单数据解析、多部门账单智能拆分、Excel格式标准化、批量邮件推送全流程自动化。适配企业复杂数据表格式、多抄送邮箱、异常容错等真实业务场景，可一键批量完成月度账单处理，**自动批量发送300余封部门对账邮件**，彻底替代人工整理对账、逐一对部门发送邮件的繁琐流程，将月度对账分发耗时从2小时压缩至3分钟，整体工作效率提升95%，全程实现账单分发零错发、零漏发，有效降低行政重复工作量与人工操作失误率。



这两个 Python 文件共同实现了**自动化生成滴滴用车费用对账单并批量发送邮件**的功能，是一个面向企业行政场景的自动化工具，以下是分文件的详细功能拆解：

这两个 Python 文件共同实现了

### 1\. UI 美化\.py（可视化操作界面）

这是基于`customtkinter`开发的图形化操作界面（GUI），核心作用是为用户提供友好的交互入口，无需编写代码即可完成操作，主要功能包括：

#### 核心功能

- **界面基础配置**：设置窗口标题、尺寸、外观主题（浅色 \+ 蓝色主题），支持窗口缩放。

- **信息输入**：

    - 邮箱账号 / 密码输入（密码支持 “显示 / 隐藏” 切换，保护隐私）；

    - 两个关键文件的选择：「滴滴 BU 部门映射表」（Excel）、「本期订单对账单」（Excel），选择后自动显示文件名，并统一文件路径为正斜杠。

- **进度与状态反馈**：

    - 运行进度条：实时展示处理进度（0\-100%）；

    - 状态提示栏：显示操作状态（如 “已选择文件”“正在处理数据”“处理完成 / 失败”），并通过不同颜色区分状态（成功绿色、警告橙色、错误红色）。

- **核心触发逻辑**：

    - 点击 “开始运行” 按钮后，先校验邮箱和文件是否完整，再通过多线程调用`version2\.py`的核心逻辑（避免界面卡死）；

    - 运行过程中禁用 “开始运行” 按钮，完成后恢复；

    - 处理完成后展示详细统计：文件生成总数 / 成功 / 失败、邮件发送总数 / 成功 / 失败，以及文件保存路径。

- **路径处理**：自动根据映射表路径生成 “部门账单 / 上月月份” 文件夹，用于存放生成的对账单。

### 2\. \[version2\.py\]\(version2\.py\)（核心业务逻辑）

这是工具的 “后台引擎”，封装了数据处理、文件生成、邮件发送的核心逻辑，被`UI美化\.py`调用，主要功能分为 4 个模块：

#### 模块 1：时间参数初始化（`init\_time\_params`）

- 自动计算时间维度：本月 / 上月的起始 / 结束日期、展示用的年份 / 月份（如 1 月自动对应去年 12 月），为账单标题、邮件内容提供时间基准。

#### 模块 2：数据加载与预处理（`load\_and\_process\_data`）

- 读取两个 Excel 文件：

    - 「本期订单对账单」：无表头，读取 “本期订单” sheet，后续用第一行作为表头；

    - 「滴滴 BU 部门映射表」：有表头，读取 “对账表” sheet；

- 构建列名索引映射（适配含全角空格的列名，如 “邮箱”“　邮箱”），方便后续按列名定位数据；

- 对映射表去重（按 SITE \+ 部门），保留唯一的部门 \- 站点 \- 邮箱映射关系。

#### 模块 3：按部门生成对账单（`generate\_department\_bill`）

- 遍历去重后的部门 \- 站点数据，为每个部门生成专属 Excel 对账单，包含 3 个 sheet：

    - 「本期账单」：该部门上月所有滴滴订单明细；

    - 「账单金额 TOP10」：该部门订单金额前 10 的明细；

    - 「免审账单」：该部门含 “免审” 关键词的订单明细；

- 样式统一：表头加粗、固定列宽（25 字符）、字体为 Calibri 11 号；

- 关键校验：收件人邮箱为空时跳过生成，避免无效文件；

- 统计生成结果：返回生成成功的文件列表（含文件路径、收件人 / 抄送邮箱等信息）、生成失败数量。

#### 模块 4：批量发送邮件（`send\_bill\_email`）

- 读取 UI 传入的邮箱账号 / 密码，连接企业 SMTP 服务器（[outlook\.wuxiapptec\.com](https://outlook.wuxiapptec.com)）；

- 为每个生成的对账单构建邮件：

    - 邮件主题：含年份、月份、站点、部门（如 “2026 年 11 月滴滴用车费用对账单 \- 上海\_行政部”）；

    - 邮件正文：固定模板（费用确认、审批提醒、反馈截止时间等），格式为 HTML；

    - 附件：添加对应部门的 Excel 对账单；

    - 收件人 / 抄送：从映射表读取，自动处理多抄送邮箱拆分；

- 发送校验：收件人为空时跳过发送，发送成功 / 失败返回布尔值，便于 UI 统计结果；

- 异常处理：捕获发送错误，打印详细日志（含堆栈信息），不中断整体流程。

### 整体业务流程

1. 用户在 UI 输入邮箱账号 / 密码，选择映射表和对账单；

2. 点击 “开始运行”，UI 调用`version2`的时间初始化→数据加载→按部门生成对账单→批量发送邮件；

3. 实时更新进度条和状态提示，最终展示生成 / 发送的统计结果；

4. 生成的对账单自动保存到 “映射表路径 / 部门账单 / 上月月份” 文件夹。

### 核心特点

- 自动化：从数据处理、文件生成到邮件发送全流程自动化，替代人工操作；

- 容错性：关键节点（邮箱为空、文件读取失败、邮件发送失败）有校验和异常处理，单个失败不影响整体；

- 可视化：UI 界面降低使用门槛，进度和状态直观反馈；

- 适配性：支持含全角空格的列名、自动处理路径分隔符（统一为正斜杠），兼容 Windows 系统。

> （注：文档部分内容可能由 AI 生成）
